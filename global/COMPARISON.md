# Сравнение: old-global__css.html → ikon-global__css.html

## 📋 ИТОГОВЫЕ ОТЛИЧИЯ И НОВОВВЕДЕНИЯ

---

## 1. ✅ СЛОИ CSS (@layer)

### OLD:
```css
@layer reset, base, components, utilities;
```

### IKON (НОВО):
```css
@layer reset, base, utilities, help;
```

**ИЗМЕНЕНИЕ**: Убран слой `components`, добавлен слой `help`

---

## 2. 🎨 ПЕРЕМЕННЫЕ ЦВЕТОВ И ПАРАМЕТРОВ

### Раздел: `:root`

#### OLD:
```css
--color-base: rgb(25, 25, 25);
--color-base-contrast: rgb(35, 35, 35);
--color-base-contrast: rgb(35, 35, 35);
--color-neutral: rgb(205, 205, 205);
--color-alternate: rgb(255, 255, 255);
--color-alternate-contrast: rgb(245, 245, 245);
--color-opacity-05: rgba(25, 25, 25, 0.05);

/* Не меняются при изменении темы */
--color-base-static: rgb(25, 25, 25);
--color-alternate-static: rgb(239, 239, 239);
--color-accent: rgb(31, 49, 241);
--color-grid-guids: rgb(255, 0, 0);

/* Style Guide */
--color-styleguide-blue: rgb(31, 49, 241);
--color-styleguide-blue-light: rgb(230, 230, 255);
--color-styleguide-red: rgb(255, 0, 0);
--color-styleguide-red-light: rgba(255, 0, 0, 0.1);

/* Габариты для хедера/меню */
--header-height: 6rem;
--header-footer: 5rem;

/* Система размеров */
--size-3xs: 0.2rem;
--size-xxs: 0.4rem;
--size-xs: 0.8rem;
[... и т.д. до size-10xl ...]

/* Анимации */
--animation-timing-function: cubic-bezier(0.25, 1, 0.5, 1);
--animation-duration-short: 300ms;
--animation-duration-medium: 500ms;
--animation-duration-long: 1000ms;
```

#### IKON (ПОЛНОСТЬЮ ПЕРЕРАБОТАНО):
```css
/* Цвета */

/* Для проекта */
--color-base: #363636;
--color-base-50: rgba(54, 54, 54, 0.5); /* ОШИБКА: исправлен синтаксис rgba */
--color-grey-btn: #cdcdcd;
--color-bg: #f5f5f5;
--popup-bg: #282828;
--color-bg-static: #f5f5f5;

/* Только для светлой темы */
--color-light-grey: #e2e2e2;

/* Только для черной темы */
--color-white-el-30: rgba(255, 255, 255, 0.3);
--color-white-el-10: rgba(255, 255, 255, 0.1);
--color-white-10: rgba(255, 255, 255, 0.1);

/* Эффект */
/* --blur: blur(30rem); */

/* Для стайлгайда */
--color-alternate-contrast: rgb(20, 20, 20);
--color-base-contrast: rgb(35, 35, 35);
--color-neutral: rgb(205, 205, 205);
--color-opacity-05: rgba(25, 25, 25, 0.05);
--color-styleguide-blue: rgb(31, 49, 241);
--color-styleguide-blue-light: rgb(230, 230, 255);
--color-styleguide-red: rgb(255, 0, 0);
--color-styleguide-red-light: rgba(255, 0, 0, 0.1);

/* Габариты для хедера/меню */
--header-height: 8.6rem; /* измененные значения */
--header-footer: 10.6rem;

/* Система размеров: УДАЛЕНа */

/* Радиус */
--radius: 0.4rem; /* НОВО */

/* Анимации */
--animation-timing-function: cubic-bezier(0.3, 0.7, 0.05, 1); /* ИЗМЕНО */
--animation-duration-short: 0.35s; /* ИЗМЕНО */
--animation-duration-medium: 0.6s; /* ИЗМЕНО */
--animation-duration-long: 0.8s; /* ИЗМЕНО */
--animation-duration-super-long: 2s; /* НОВО */
--animation-duration-instant: 0.2s; /* НОВО */
--animation-settings: 20s infinite linear; /* НОВО */
```

**ИТОГОВЫЕ ИЗМЕНЕНИЯ В ПЕРЕМЕННЫХ**:
- ✅ Переход с `rgb()` на `hex` и `rgba()` форматы для основных цветов
- ✅ Добавлены проектные цвета: `--color-base-50`, `--color-grey-btn`, `--color-bg`, `--popup-bg`, `--color-bg-static`, `--color-light-grey`, `--color-white-*`
- ❌ УДАЛЕНЫ переменные размеров системы (`--size-*`)
- ✅ Добавлены: `--radius`, `--animation-duration-super-long`, `--animation-duration-instant`, `--animation-settings`
- ✅ ИЗМЕНЕНЫ значения высот хедера: 6rem → 8.6rem, 5rem → 10.6rem
- ✅ ИЗМЕНЕНЫ timing функции анимации
- ✅ ИЗМЕНЕНЫ длительности анимаций (с ms на s)

---

## 3. 🌓 ТЕМЫ (dark-theme, light-theme)

### OLD:
Только `.dark-theme` класс

### IKON (НОВО):
Добавлен класс `.light-theme` с явным определением светлой темы

```css
.light-theme {
  --color-base: #363636;
  --color-base-50: rgba(54, 54, 54, 0.5);
  --color-grey-btn: #cdcdcd;
  --color-bg: #f5f5f5;
}
```

---

## 4. 📱 МЕДИА-ЗАПРОСЫ ДЛЯ ПЕРЕМЕННЫХ

### OLD:
Только `html { font-size: ... }` в медиа-запросах

### IKON (НОВО):
Добавлены медиа-запросы для `:root` с пересчетом высот хедера:

```css
@media screen and (max-width: 991px) {
  :root {
    /* ОШИБКА: изменено с html на :root для корректного применения переменных */
    --header-height: 4.1rem;
  }
}

@media screen and (max-width: 479px) {
  :root {
    /* ОШИБКА: изменено с html на :root */
    --header-footer: 10.6rem;
    --header-height: 5.1rem;
  }
}
```

---

## 5. 🖼️ НОВЫЙ РАЗДЕЛ: ОПТИМИЗАЦИЯ ШРИФТОВ И ТЕКСТА

### IKON (ПОЛНОСТЬЮ НОВО):
```css
/* Лучшие настройки для сглаживания шрифтов */
html {
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-smooth: always;
  shape-rendering: geometricPrecision;
  background-color: var(--color-bg);
}

/* Дополнительные улучшения для текста */
body {
  font-kerning: normal;
  font-variant-ligatures: common-ligatures;
  font-feature-settings: "kern", "liga", "clig", "calt";
}

/* Для контента с анимациями */
.animated-content {
  /* Временно отключаем сглаживание во время анимаций */
}
```

---

## 6. 🔄 АДАПТИВНОСТЬ (font-size в html)

### OLD:
```css
html {
  font-size: 0.733vw;
}
```

### IKON (ДОБАВЛЕНА ОШИБКА ДОКУМЕНТИРОВАНА):
```css
html {
  font-size: 0.733vw;
  font-size: 0.625vw; /* ОШИБКА: дублирующее свойство, возможно нужно удалить одну строку */
}
```

---

## 7. 🎭 ИСЧЕЗНОВЕНИЕ: @layer components

### OLD:
Закомментированный слой `components` присутствовал:
```css
/* ====================== Компоненты ======================== */
@layer components {
  .btn:hover .btn__line,
  .btn:hover .btn__icon { ... }
  /* ... */
}
```

### IKON:
Заменен на `@layer help` в конце стилей

---

## 8. 📊 ВИДИМОСТЬ ЭЛЕМЕНТОВ (show-* классы)

### OLD:
```css
display: block !important;
```

### IKON (НОВО):
```css
display: flex !important;
```

**ИЗМЕНЕНО**: Все классы видимости переключены с `block` на `flex`

---

## 9. 📦 КОНТЕЙНЕРЫ (.cont)

### OLD:
Отсутствуют правила для контейнеров

### IKON (ПОЛНОСТЬЮ НОВО):
```css
/* Контейнеры */
.cont {
  padding-left: 1.5rem !important;
  padding-right: 1.5rem !important;
}
.cont--no-paddings {
  padding-left: 0 !important;
  padding-right: 0 !important;
}

@media screen and (max-width: 991px) {
  .cont-tablet-mobile {
    padding-left: 1.6rem !important;
    padding-right: 1.6rem !important;
  }
  .cont--no-paddings-tablet-mobile {
    padding-left: 0 !important;
    padding-right: 0 !important;
  }
}

@media screen and (max-width: 479px) {
  .cont {
    padding-left: 0.8rem !important;
    padding-right: 0.8rem !important;
  }
  .cont-mobile { /* НОВО */ }
  .cont-tablet-mobile { /* НОВО */ }
  .cont--no-paddings-mobile { /* НОВО */ }
  .cont--no-paddings-tablet-mobile { /* НОВО */ }
}
```

---

## 10. 🎨 НОВЫЕ КОМПОНЕНТЫ И ПРАВИЛА (В КОНЦЕ СТИЛЕЙ)

### IKON (ПОЛНОСТЬЮ НОВО - добавлено в конец файла):
```css
/* Projects */
.page-project__gallery .text-block-wrap-div { ... }
.page-project__gallery .tt-rich-figure { ... }

/* Hover-эффекты */
.card-img-text-1:hover .img-full { transform: scale(1.2); }
/* ... и много других компонент */

/* Об авторе */
.page-about-team__col--right { padding-right: 0 !important; }
.page-about-team__col--left { padding-left: 0 !important; }
.page-about-team-card__img-wrapper img { object-position: center 10%; }

/* И еще ~100+ строк компонент-специфичных стилей */
```

---

## 11. ⚠️ ОТМЕЧЕННЫЕ ОШИБКИ В КОДЕ

В файле `ikon-global__css.html` есть специально отмеченные комментарии об ошибках:

1. **rgba синтаксис**: `--color-base-50: rgba(54, 54, 54, 0.5); /* ОШИБКА: исправлен синтаксис rgba */`
2. **Дублирующее свойство**: Две строки `font-size` подряд
3. **translate3d регистр**: `translate3D` → `translate3d` (исправлен в коде)
4. **Медиа-запросы**: `html` → `:root` (исправлены комментарием)

---

## 📊 ОБЩАЯ СТАТИСТИКА

| Параметр | OLD | IKON | Статус |
|----------|-----|------|--------|
| Строк кода | ~250 | ~717 | ➕ +187% |
| Слои @layer | 4 (reset, base, components, utilities) | 4 (reset, base, utilities, help) | ♻️ Переставлены |
| Переменные цветов | 7 | 15+ | ➕ Значительно расширены |
| Переменные размеров | 20+ (`--size-*`) | 0 (удалены) | ❌ |
| Компонент-специфичные стили | Минимум | Максимум | ➕ Добавлены |
| Оптимизация шрифтов | Нет | Да | ✅ НОВО |
| Поддержка контейнеров `.cont` | Нет | Да | ✅ НОВО |
| Проектные цвета | Нет | Да | ✅ НОВО |
| Светлая тема | Нет | Да | ✅ НОВО |

---

## 🎯 КЛЮЧЕВЫЕ ВЫВОДЫ

✅ **Нововведения в IKON**:
1. Полная переработка система переменных (цвета → hex/rgba, размеры → удалены)
2. Поддержка светлой темы через `.light-theme`
3. Оптимизация рендеринга шрифтов
4. Система контейнеров с адаптивными паддингами
5. Смена `display: block` на `display: flex` для видимости
6. Обширная коллекция компонент-специфичных стилей
7. Улучшенные медиа-запросы для переменных

❌ **Удаления в IKON**:
1. Переменные системы размеров (`--size-*`)
2. Слой `@layer components`
3. Глобальные анимации на всех элементах (`* { transition: ... }`)

⚠️ **Потенциальные проблемы**:
1. Дублирующее свойство `font-size`
2. Комментарии об ошибках, которые уже исправлены
